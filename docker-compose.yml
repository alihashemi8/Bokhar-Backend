
services:
  drf:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      - redis

  redis:
    image: redis:7

  celery:
    build: .
    command: celery -A bokhar worker --loglevel=info
    volumes:
      - .:/app
    depends_on:
      - redis
